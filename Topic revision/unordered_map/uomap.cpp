// keys are not index, we chose them.
// this thing store value in key value pair. 

#include <iostream>
#include <unordered_map>

using namespace std;

int main(){

    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n; 
    cin >> n; 
    
    unordered_map<int, int> mp ; 
    mp[1] = 19; // we decide key, and assign value.

    unordered_map<int, int> mp;
//key value pair


    return 0;  
}  