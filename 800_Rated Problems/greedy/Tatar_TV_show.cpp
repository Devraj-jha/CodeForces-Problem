#include <iostream>
using namespace std;

int main(){

    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    
    while(t--){
        /// s lenght k integer
        // 
        int n,k; 
        cin >> n >> k; 
        string s; 
        cin >> s; 
        for(int i = 0; i < s.size() ; i++)
        {
         for(int j = i; j < n; j++){
            
         }
        }
    }
    return 0;

}

//input ->
// s of length n;
// int k;
//i to i + k; 
// invert these characters.
// o to 1 and 1 