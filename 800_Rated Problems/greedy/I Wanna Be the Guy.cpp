#include <iostream>
#include <set>
using namespace std;

int main(){

    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    set <int> x;

    int n;
    cin >> n;

    for ( int i = 0; i < n; i++ ){

        
        
    }
    if ( x.size() == n){
        cout <<"I become the guy.";
    }else {
        cout << "Oh, my keyboard!";
    }
   

    return 0;  
}
