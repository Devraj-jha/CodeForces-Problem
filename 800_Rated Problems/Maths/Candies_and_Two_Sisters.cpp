#include <iostream>
using namespace std;

int main(){

    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;
    
    while(n--){
    long long n;
    cin >> n;
    cout << (n-1)/2 <<"\n";


     }
     //7
     //now think...
     //7 = (a - 1(a)) + 1(b);
     //the moment b > a; skip. that one..
    // 7 / 2 = 3. = b, a = 4 
    }
   