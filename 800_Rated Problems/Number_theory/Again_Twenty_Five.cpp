#include <iostream>
using namespace std;

int main() {
    long long n;
    cin >> n;      // read input (required)

    // for all n >= 2, last two digits of 5^n are always 25
    cout << "25";

    return 0;
}
