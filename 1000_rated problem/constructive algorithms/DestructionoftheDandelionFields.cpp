#include <vector>
#include <iostream>
using namespace std;

int main(){

    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t; //number of test cases

    cin >> t;
    
   while (t--){
    int n;
    cin >> n;
    

       }

    return 0;  
}  

// * n fields => ith field -> ai. (dandelions)
//* if odd -> toggle state ( law mover)
//* if on -> cut all subjects

